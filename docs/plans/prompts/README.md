# Phase Prompts

- Use one prompt per phase or major checkpoint.
- Keep prompts operational and scoped to the current phase only.
- After a checkpoint, refresh the next prompt from the current code and docs instead of reusing
  stale assumptions.
